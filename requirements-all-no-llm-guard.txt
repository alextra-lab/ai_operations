# All services except llm_guard_svc (used with constraints.txt for secure transformers).
# llm_guard_svc is built separately because gliner==0.2.26 caps transformers<5.2.0,
# while the rest of the stack uses >=5.9.0 (see constraints.txt).
-r ./src/orchestrator/requirements.txt
-r ./src/embedding/requirements.txt
-r ./src/corpus_svc/requirements.txt
-r ./src/inference-gateway/requirements.txt
-r ./src/shared/requirements.txt
