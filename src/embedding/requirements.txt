# Core dependencies
fastapi>=0.138.1
uvicorn==0.49.0
pydantic>=2.12.5,<3.0.0
pyyaml>=6.0.1
openai>=2.44.0,<3.0.0  # Updated Nov 2025: Allow v2.x (current: 2.16.0)
starlette>=1.3.1
python-multipart>=0.0.32
httpx>=0.28.1
backoff>=2.2.1
tenacity>=9.1.4

asyncpg>=0.31.0
sqlalchemy>=2.0.51
psycopg>=3.3.2
psycopg[binary]>=3.3.2

# Local embedding provider (optional): torch + sentence-transformers are NOT installed
# by default — they live in requirements-local.txt and are only added when the image is
# built with WITH_LOCAL_EMBEDDING=1. This keeps the default image lean and CPU-only (no
# torch, no multi-GB nvidia-* CUDA wheels). numpy stays here: small, CUDA-free, broadly used.
numpy>=2.5.0,<3.0.0

# Instrumentation and Monitoring
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
opentelemetry-instrumentation-requests==0.64b0
opentelemetry-instrumentation-sqlalchemy==0.64b0
prometheus-client==0.25.0

# Authentication
python-jose>=3.3.0  # JWT validation
passlib[bcrypt]>=1.7.4  # Password hashing
