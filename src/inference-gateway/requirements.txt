# Core FastAPI stack (matching backend service)
fastapi>=0.117.1
uvicorn==0.48.0
starlette>=0.48.0
pydantic>=2.12.5,<3.0.0

# Authentication (reuses shared.auth) - Needed for P1-T3
python-jose[cryptography]>=3.3.0  # JWT tokens - matches embedding service
bcrypt>=5.0.0,<6.0.0
passlib==1.7.4

# Database - Needed for P1-T2
sqlalchemy>=2.0.43
psycopg>=3.3.2
psycopg[binary]>=3.3.2
asyncpg>=0.31.0

# HTTP clients for provider calls - Needed for P1-T5+
httpx>=0.28.1
openai>=2.16.0,<3.0.0

# Retry logic - Needed for P2-T3
backoff>=2.2.1

# Redis for rate limiting and circuit breaker - P2-T4
redis>=5.0.0

# Logging (reuses shared.logging_utils)
structlog>=24.1.0  # Structured logging - version constraint for security

# Validation - Needed for P2-T2+
jsonschema>=4.26.0,<5.0.0  # Prevent v5 breaking changes
pyyaml>=6.0.1

# Utilities
python-multipart>=0.0.6
